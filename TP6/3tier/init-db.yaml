apiVersion: v1
kind: ConfigMap
metadata:
  name: init-db
data:
  init.sql: |
    CREATE TABLE IF NOT EXISTS reviews (
      id SERIAL PRIMARY KEY,
      content TEXT
    );

    INSERT INTO reviews (content) VALUES
    ('Imagine tu supprimes le pod de vs-code sans faire exprès'),
    ('Nan je rigole'),
    ('Mais imagine quand même'),
    ('Oh non j'ai tué Python!');